# Diamond HQ — I2V first-last-frame (VACE blocked by 64GB memory ceiling)

Plate(8K) last frame -> dive into diamond -> couple -> (gen2) smile/reach/finger.
Full sampling (no Lightning), Q8. Iteration res 512; final will be higher + SeedVR2 + grain, 24 & 29.97fps.

## 01 - dive (gen#1, 512 iteration)

## 02 - FULL sequence preview (gen1 dive + gen2 continuation), 512 iteration
plate->dive->couple->smile->finger fills view. Proven; high-res final next.
